[tasks.fmt]
description = "Format project source code"
depends = ["*:*fmt"]

[tasks."fmt:check"]
description = "Check source formatting"
depends = ["*:fmt:check"]

[tasks."sort:crates"]
description = "Sort Cargo manifest dependencies"
run = "cargo sort --workspace --grouped --order package,dependencies,features"

[tasks."sort:crates:check"]
description = "Check Cargo manifest dependency ordering"
run = "cargo sort --workspace --grouped --order package,dependencies,features --check"

[tasks.lint]
description = "Run all lint checks"
depends = ["*:lint:*"]

[tasks."deps:check"]
description = "Check dependency policy"
depends = ["*:deps:check"]

[tasks.check]
description = "Check project source code"
depends = ["*:check:*"]

[tasks.test]
description = "Run tests"
alias = "t"
depends = ["*:test"]

[tasks."test:integration"]
description = "Run all service-dependent integration tests (requires NATS and Docker)"
depends = ["test:integration:nats", "test:integration:s2"]

[tasks."pre-commit"]
description = "Run the full local development gate"
alias = "prk"
depends = [
  "sort:crates:check",
  "fmt:check",
  "lint",
  "deps:check",
  "check",
  "test",
]
